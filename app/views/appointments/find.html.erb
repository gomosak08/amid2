<!-- app/views/appointments/find.html.erb -->

<div class="container mx-auto px-4 py-8 max-w-md">
  <h2 class="text-2xl font-bold mb-4 text-center">Maneja tus Citas</h2>
  <p class="text-gray-700 mb-6 text-center">Ingresa tu código único de cita a continuación para cancelar tu cita.</p>

  <%= form_with url: find_appointments_path, method: :post, local: true, class: "space-y-4" do |form| %>
    <!-- Campo del Código de Cita -->
    <div>
      <%= form.label :unique_code, "Código de Cita", class: "block text-lg font-medium text-gray-700 mb-2" %>
      <%= form.text_field :unique_code, class: "block w-full p-2 border border-gray-300 rounded-md shadow-sm focus:ring-blue-500 focus:border-blue-500" %>
    </div>

    <!-- Botón de Enviar -->
    <div class="text-center">
      <%= form.submit "Buscar Citas", class: "w-full bg-blue-600 text-white py-2 px-4 rounded-md hover:bg-blue-700 transition-colors duration-200" %>
    </div>
  <% end %>
</div>
