<!-- app/views/admin/packages/edit.html.erb -->
<div class="max-w-4xl mx-auto px-4 py-8">
  <!-- Page Title -->
  <h1 class="text-3xl font-bold text-gray-800 mb-6">Editar Paquete</h1>

  <!-- Package Form -->
  <div class="bg-white p-6 rounded-lg shadow-md">
    <%= render 'form' %>
  </div>

  <!-- Back Link -->
  <div class="mt-6 text-center space-x-4">
    <%= link_to 'Regresar a Paquetes', admin_packages_path, 
           class: 'bg-blue-600 text-white py-2 px-4 rounded hover:bg-blue-700 transition-colors duration-200' %>
  </div>
  

</div>
