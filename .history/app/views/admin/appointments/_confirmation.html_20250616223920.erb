<div class="p-6 bg-green-50 border border-green-200 rounded-md">
  <h2 class="text-2xl font-bold mb-4">¡Cita agendada con éxito!</h2>

  <p><strong>Paciente:</strong> <%= @appointment.name %></p>
  <p><strong>Edad:</strong> <%= @appointment.age %></p>
  <p><strong>Médico:</strong> <%= @appointment.doctor.name %></p>
  <p><strong>Fecha y hora:</strong>
    <%= @appointment.appointment_date.strftime("%d/%m/%Y") %> a las
    <%= @appointment.time_slot.strftime("%H:%M") %>
  </p>
</div>
